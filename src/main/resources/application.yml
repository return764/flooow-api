spring:
  datasource:
    username: postgres
    password: postgres
    url: jdbc:postgresql://localhost:5432/postgres

  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
debug: false
